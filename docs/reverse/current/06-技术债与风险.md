# 06 — 技术债与风险清单(16 项)

> 基线 `ff622a7`(v2.0.0) | 2026-08-29 | 只记录不修改;修复需单独授权排期。
> 格式: 现象(证据) → 影响 → 建议。

| # | 级别 | 现象与证据 | 影响 | 建议 |
|---|------|-----------|------|------|
| 1 | 高 | 前端三套并存且定位混乱: ①生产在用 `frontend/demo/index.html`(原生JS,与 WD 根 `demo/` 一致);②WD 源头 `frontend/src` 是对接新后端的 TS 管理端 `training-admin-frontend`(`router/index.ts`,**从未部署**);③服务器 `frontend/src` 是旧 JS 版,调已删除的旧端点(`src/api/auth.js:4`) | 新人误解生产真身;已开发完成的新管理端被闲置;README/AGENTS 旧描述失真 | **需拍板**: 部署 TS 管理端替代 demo SPA,或明确 demo SPA 为长期方案并把 TS 端归档 |
| 17 | 中 | **部署漂移**(2026-08-29 经 WD 工作区 md5 交叉验证): ①`frontend/src` 两边不同(见#1);②`routers/teacher.py` 删除顺序实现不同(WD 先删 Teacher 后删 User / 服务器先注销 User 后删 Teacher,11 行);③`gunicorn_conf.py` timeout 120(WD)vs 300(服务器);④WD 的 Dockerfile/docker-compose/migrations/001 SQL/migrate_to_pg.py 均未用于生产 | WD 与生产代码不再是同一份;"以哪边为准"无答案 | 建立"WD → git → 服务器"单向同步流:以后改动只在本仓库(git main)进行,部署走 quick-sync,WD 目录封存为历史参考 |
| 2 | 高 | tests/ 不可运行: `tests/conftest.py:10-17` import 旧布局 main/database/seed_data | 质量保障为零 | 按新 `app.main:app` 重写 conftest + 冒烟 |
| 3 | 中 | `backend/app/batch1_smoke.py` 冒烟脚本混入应用包 | 会被打包部署 | 移出 app 包 |
| 4 | 高 | 依赖缺失: `python-docx`、`qrcode` 被 import 但不在 requirements.txt(`template_engine.py:25-31` 缺包直接 SystemExit;`answer_sheet_renderer.py:18` 顶层 import qrcode) | 按清单新装环境**启动失败/500** | 补入 requirements.txt |
| 5 | 中 | `routers/import_export.py:34-36` 把项目根塞 sys.path 后 import `question_import_export`,实际模块在 `backend/` 下,仅 CWD=backend 时侥幸可用 | 换启动方式即崩 | 改为包内相对导入或修正 sys.path 指向 backend/ |
| 6 | 高 | 密码无盐 sha256 三处(`auth.py:32`、`user.py:41`、`teacher.py:44`);bcrypt 已装未用;默认弱口令 admin/admin123(`seed.py:186`) | 安全风险 | 换 bcrypt/argon2 + 强制改密策略 |
| 7 | 高 | AI api_key 前端明文存 localStorage 随请求传输(`ai.py:7`);教研云账密/令牌明文落盘(`jiaoyanyun_credentials.json`/`jiaoyanyun_token.json`,已 gitignore) | 凭证泄露面大 | 服务端密钥管理;凭证收敛到 .env/密钥文件 |
| 8 | 高 | AI 评分是满分占位桩(`exam.py:312-355`,ai_confidence=0.9 写死) | 评分数据失真 | 接真实视觉评分模型,替换桩 |
| 9 | 中 | 三个统计表(student/class/task_statistics)字段齐备但无写入方,看板全部实时聚合(`exam.py:390`、`student.py:221`、`class_router.py:190`) | 僵尸 schema 误导开发 | 二选一: 废表或启用落库 |
| 10 | 中 | audit_logs 仅存在于模型注释(`system_models.py:5-8`),无表无端点;demo SPA「操作日志」与 Vue AuditLog 无后端支撑 | 审计需求悬置 | 建表建端点或移除入口 |
| 11 | 低 | 版本断链: `bump_version.sh:11` 引用已删除的 `backend/main.py`;`app/main.py:57` version="0.1.0" 与 VERSION=2.0.0、package.json 1.0.0 不一致;CHANGELOG 停在 v1.1.x | 版本管理混乱 | 统一版本源(VERSION 单源) |
| 12 | 中 | 旧 Vue SPA 错误上报打 `/api/logs`(`request.js:25`、`main.js:29`),新端点是 `/api/v1/system-logs` 且字段不同 | Vue 端错误进不了新日志表 | 随旧 SPA 退役一并处理 |
| 13 | 低 | 根目录杂物: `ai_learning.db`(4KB 旧库残留)、根 `demo/` 与 `frontend/demo/` 两套演示资源;`meta.py:102-112` 发号注释与实现口径不一致 | 认知噪音 | 清理杂物;统一发号逻辑 |
| 14 | 低 | 旧文档失真: deploy.sh:144 输出 /api/docs(实际 404);cloud_migration.md 描述的 asyncpg/MinIO/alembic 与现状不符 | 误导 | 本套逆源文档为准,旧文档归档 |
| 15 | 中 | 删除级联全部手写多表 delete(`paper.py:253-304`、`exam.py:188-219`、`student.py:141-181`),仅 paper_templates 有 DB 级联;tag 删除全表扫描改 JSON(`tag.py:96-100`) | 漏删即孤儿数据;性能隐患 | 引入 DB 外键级联或封装删除服务 |
| 16 | 中 | 两套登录/角色模型冲突: 新后端仅 admin/teacher,旧 Vue SPA/AGENTS 仍写 research/super;demo SPA 离线回退可绕过后端进界面(`index.html:1211-1244`) | 演示数据与真实数据混淆风险 | 明确单角色模型;离线回退加显眼水印或移除 |

## 优先修复建议(Top 5)
1. **#4 补依赖**(python-docx、qrcode)—— 一行修复,消除环境重建风险
2. **#8 AI 评分桩落地** —— 核心业务真实性
3. **#6 密码哈希升级** —— 安全底线
4. **#1 前端收敛决策** —— 需要用户拍板:WD 里已有一套对接新后端的 TS 管理端,是部署它替代 demo SPA,还是继续深耕 demo SPA
5. **#2 测试重写** —— 保障后续迭代

## 附:WD 交叉验证结论(2026-08-29)

- ✅ 验证成立: backend/app 全部 core/models/schemas + 17 个 router 逐字节一致;demo 前端与教研云三脚本逐字节一致;requirements.txt 一致(债 #4 成立);95 端点数不受 teacher.py 差异影响
- 🔧 已修正: frontend/src 定性(原误判"旧遗留",实为 WD 内的 TS 新管理端未部署)
- ➕ 新增: 债 #17 部署漂移(teacher.py 删除顺序、gunicorn timeout、Docker 工件未采用)
